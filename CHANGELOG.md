# 更新日志

本文件记录每次发布的版本与更新内容，**最新版本放在最上方**。
每完成一个需求，就在最新版本条目下加一行简短说明（一句话即可）。

格式约定（发布脚本 `backend/deploy/release.sh` 依赖它解析）：

- 每个版本一个 `## 版本号` 标题，版本号形如 `0.2.0+2`（`x.y.z` 是展示版本名，
  `+N` 是 Android versionCode，必须递增）。
- 标题下用 `-` 列出本次更新内容，每条一句话。
- 顶部第一个 `## ` 条目即「待发布/最新」版本，发布脚本只读它。
- 顶部版本号必须与 `pubspec.yaml` 的 `version:` 一致（脚本会校验，不一致会拒绝发布）。

---

## 0.3.16+35

- 旅行:重做交互——添加只需选城市 + 填具体地点(可选)+ 传图保存,保存后后台自动把照片生成「色彩漫游」记录图(无需手填提示词、不用干等);地图放大点城市缩略图进图集,可全屏放大查看、下载到相册、删除记录

## 0.3.16+34

- 修复:旅行「一句话重绘」生成图片时常误报「请求超时」——图生图较慢,前端等待上限从 15 秒放宽到 180 秒(后端早已成功出图,只是前端等不及);提示词长度上限也放宽

## 0.3.16+33

- 修复:旅行地图缩放时在部分机型(Adreno/Vulkan)闪退——省界改为预渲染纹理,平移缩放不再每帧重绘矢量,稳定不崩
- 旅行:放大时不再铺满全部城市名(太挤),只保留城市小点;名字只标在你去过的城市上

## 0.3.16+32

- 新增「旅行」插件:一张可拖动缩放的真实中国地图,把每段旅行钉在城市上(放大后城市旁浮出照片缩略图);添加记录支持「用一句话重绘这张照片」——AI 图生图换画风/心情,原图始终保留、二选一

## 0.3.15+31

- 新增「AI 集成设置」(我的 → 设置):按家庭、按类型(多模态 / 文本 / 图片生成 / 视频生成)配置 AI 模型与密钥,插件按类型自动调用;密钥加密保存、不再写死在服务器,未配置时给出清晰提示

## 0.3.14+30

- 优化:申请屏幕高刷新率——此前 Flutter 默认锁 60fps,90/120Hz 屏上动画偏卡;现在跑满屏幕刷新率,滑动与转场更顺滑(60Hz 屏无影响)

## 0.3.13+29

- 优化:带图详情页(回忆 / 植物日记 / 菜谱等)加载更顺滑——图片按显示尺寸解码、并等卡片形变转场结束后再加载,消除加载时的掉帧卡顿

## 0.3.12+28

- 首页:点开任意插件卡片改为卡片放大形变成详情页、返回缩回卡片的转场(所有插件生效),与回忆一致

## 0.3.11+27

- 回忆:首屏加载改用骨架屏(替代转圈)、卡片错峰淡入上滑入场、点开详情改为卡片形变放大转场,翻看更顺滑
- 回忆:本地缓存内容与照片、启动时后台预热,从主页点进回忆基本秒开、不再卡顿

## 0.3.10+25

- 选择日期时的日历弹窗改为简体中文（月份、星期、确定/取消等不再显示英文）

## 0.3.9+24

- 新增「到期管家」插件:把身份证、护照、签证、驾照、车险、年检、合同、会员卡等会到期的东西记在一处,到期前按设定的提前天数提醒全家,过期后再提醒一次
- 记账:新增「拍小票」——拍小票 / 账单 / 付款截图,AI 自动识别金额、分类、商家并预填「记一笔」表单,确认即入账

## 0.3.8+23

- 看电影:简介 / 评分 / 海报改为从 TMDB(themoviedb.org)获取(替换原先 AI 凭记忆生成的豆瓣数据),评分标签相应改为「TMDB」
- 看电影:新增「片库」——按类型多选 + 排序(热门 / 高分 / 最新)浏览 TMDB 影库,点开详情可一键加入「想看」(自带简介、评分、海报)

## 0.3.7+22

- 设置-清除缓存:进入后展示应用占用总大小及构成(数据缓存/历史安装包/应用数据),可分别清理「数据缓存(图片、临时文件)」与「历史下载的安装包」

## 0.3.6+21

- 新增「退休倒计时」插件:管理家庭资产(存款/公积金)与负债(房贷/车贷),设退休日期与存款目标,算出还需多少月达标、每月还需提高多少(+盈余/−缺口)
- 退休倒计时:每月固定收入按各账户入账日自动入账、负债按扣款日自动减负债减存款,并打通「记账」每月把上月支出从存款结算扣除(全部记入流水)

## 0.3.5+20

- 植物日记:支持删除养护记录(连同照片);进行中的 AI 分析随之停止;若封面来自被删记录则自动改用其余记录

## 0.3.4+19

- 植物日记:一条状态记录支持上传多张照片(最多 6 张),AI 综合所有照片一起分析

## 0.3.3+18

- 植物日记:支持编辑已添加植物的名称 / 品种 / 摆放位置(详情页右上角铅笔图标)

## 0.3.2+17

- 植物日记:主页新增天气卡片,显示当前位置名 + 和风天气全部字段(温度/体感/湿度/降水/气压/能见度/云量/露点/风向风力风速/紫外线等)
- 植物日记:定位修复——授权后即时取「上次已知位置」,不再卡在「定位中」;默认位置可显示并起名(全家共享)
- 植物日记:AI 看图分析超时放宽到 180s、详情页等待结果窗口延长到约 2 分钟,减少「分析失败」

## 0.3.1+16

- 植物日记:摆放位置标签可自定义增删、全家共享,内置「阳台」改为「南阳台」(标签会作为环境信息参与 AI 分析,写具体些更准)

## 0.3.0+15

- 新增「植物日记」插件:给家里每株植物建档、拍照记录,AI 结合照片与当地天气分析养护状态,给出浇水/施肥/修剪建议
- 植物日记支持用户设定浇水/施肥周期(AI 给建议值可一键采纳),到期自动提醒
- 新增共享天气服务模块(和风天气)与 AI 多模态(看图)能力,供各插件复用

## 0.2.0+14

- 修复「回忆」性能问题:看完大图返回后所有缩略图重新加载、很卡。原因是缩略图按原图全分辨率(长边 2400px)解码进内存,几张就撑爆图片缓存把缩略图挤掉;改为按实际显示尺寸解码(缩略图小图、大图按屏幕分辨率),返回不再整屏重载
- 修复「回忆」从详情返回时间线时整屏闪一下、缩略图重新加载:之前返回会把时间线重置成加载中再重建;改为首屏才显示加载态,之后的刷新静默后台更新、保留当前列表就地替换

## 0.2.0+13

- 完善账号与安全:新增登录密码。注册新号码 / 已有账号首次登录都需设置密码;登录页加密码输入框,后端只存 scrypt 不可逆哈希(加盐),绝不保存明文。「设置 · 账号与安全」下新增「修改密码」(原密码 + 新密码两次确认)
- 完善账号与安全:请求认证改用登录签发的会话令牌(Bearer),后端只存令牌哈希、可撤销——退出登录即刻失效;生产环境不再接受可伪造的 X-User-Id 旧通道。旧版本需更新到本版重新登录

## 0.2.0+12

- 新增「订阅管家」💳 插件：记录订阅 / 定期账单(视频会员、云盘、房租、宽带等),按月或按年设金额与首次扣费日;到期前自动提醒,到期当天若开启「自动记账」且家庭装了「记账」,会自动把这笔扣费记入账本(订阅分类)并把下次扣费日顺延一个周期;可暂停、可关自动记账。首页卡片显示下一笔最近的扣费
- 「家历」编辑页新增「存入手机日历」开关：保存带日期的日程后弹出手机自带日历的「添加事件」页（预填标题/时间/重复），提醒时间由用户在系统页里自定义；Android 走系统 intent 免授权，iOS 走 EventKit 编辑页
- 新增 AI 通用模块（接 Kimi）：插件可统一调用大模型获取返回内容，支持思考模型；首个落地场景是「看电影」——新增电影只填片名,保存后后台自动用 AI 补上 100-150 字剧情简介、豆瓣评分,并抓取豆瓣海报存到对象存储;列表展示海报缩略图 + 评分 + 简介,补充中有状态提示、失败可一键重试
- 新增「家历」📅 插件：全家共享的日程 + 待办。一条条目选了日期就是日程（可再指定时间），不选日期就是无日期待办；支持每天 / 每周 / 每月重复（重复项「完成」会自动顺延到下次，不被划掉），可指派给家庭成员（显示真实头像）、按「过期 / 今天 / 明天 / 本周 / 以后」分组的议程视图，并能在到期前 N 天推送提醒。首页卡片显示下一件该做的事
- 「记账」新增「软件/订阅」💳 标签（前端 + 后端白名单同步）
- 「囤货铺」新增每周盘点提醒：每周六 20:00 给装了该插件的家庭推送通知，提醒全家清点囤货、补进采买清单（后端定时任务，点通知直达囤货铺）

## 0.2.0+11

- 「记账」记一笔金额输入框改为自绘计算键盘：不再调起系统输入法，支持 `+ − × ÷` 四则运算与连续表达式、AC / ⌫ / =，右下角「完成」直接保存

## 0.2.0+10

- 新增「看电影」🎬 插件：简单记录想看的电影 / 看过的电影,两个 tab 切换;每条电影只有片名 + 备注,点圆圈一键标记看过,长按删除。首页卡片显示最新想看的一部 + 还剩几部
- 修复「回忆」新上传的照片在时间线上不显示：App 全局 HTTPS 信任链原本只信自签 CA，导致后端把 `/raw` 302 重定向到腾讯云 COS 时 TLS 握手失败；改为同时信任自签 CA + 系统公共根证书后，新图片能正常加载（旧图只是先前缓存命中才看着正常）
- 从「添加插件」市场下架「家庭相册」插件：后端 routes / models 都有，但前端尚未实现详情页，装上点开是死的；以后补齐再上架（PluginManifest 加 `published` 标记，已装该插件的家庭不受影响）

## 0.2.0+9

- 首页「回忆」4×2 大卡片右侧新增图片轮播：每 4 秒淡入淡出切换一张，循环展示最近 5 张照片
- 「回忆」大图页长按图片可保存到系统相册（图片会归到「窝」相册分组里）
- 「记账」新增「零食」🍭 标签
- 「家务活」新建时 emoji 候选里加上 🐟（喂鱼 / 清鱼缸用得上）
- 「菜谱」详情页食材区右上角新增「加入采买」按钮：弹层选要加哪些食材（默认全选，可单独勾选 / 全不选），一键加入「囤货铺」的采买清单，自动带上原食材的数量备注「来自菜谱『XXX』」
- 首页顶部新增「采买未完成」提醒条：囤货铺采买清单还有待买项时显示总数，按来源（食材采买 / 囤货采买）分组；只有一类时直接跳进囤货铺采买页，两类同时存在时点击弹气泡展开各自数量，再点击对应行跳转

## 0.2.0+8

- 「回忆」插件的照片/视频改存腾讯云 COS：上传依旧经后端鉴权落到对象存储，读取时后端鉴权后 302 重定向到 1 小时有效期的预签名 URL，从此图片直接从 COS 拉，不再吃后端带宽，浏览明显变快
- 「回忆」上传压缩调整：长边从 1600 提到 2400，质量从 82 提到 92，画质更接近原图（单张体积仍可控）

## 0.1.0+7

- APK 发布迁移到腾讯云 COS：APK 直接从对象存储公共域名下载，不再吃后端单机带宽，应用内更新明显变快（配 `COS_*` 环境变量后生效，未配置则回退本地盘）

## 0.1.0+6

- 新增「囤货铺」插件：记录家庭囤货与共享采买清单两个联动列表；囤货可加减数量、设低量阈值，见底时一键补进采买，采买买到后还能顺手入库；首页卡片在有东西要补货时提醒，平时显示采买清单剩几项

## 0.1.0+5

- 回忆：点击图片可全屏看大图，支持左右滑动浏览上一张 / 下一张（视频则点击播放）
- 回忆：时间线、详情与留言里显示创建者的真实头像（未设头像时回退到 emoji 头像）
- 记账：账单列表里显示记录人的真实头像（未设头像时回退到 emoji 头像）
- 菜谱：详情页「由谁添加」显示作者真实头像（未设头像时回退到 emoji 头像）
- 家务：列表负责人与指派选择器显示真实头像（未设头像时回退到 emoji 头像）
- 家庭管理：成员列表与角色操作里显示成员的真实头像（未设头像时回退到 emoji 头像）

## 0.1.0+4

- 新增「回忆」插件：以贯穿的时间线记录生活，可上传照片或短视频、写标题和文案，另一半还能留言；首页卡片展示最近一段回忆的标题与总条数
- 回忆：新建时上传的第一张照片会自动读取拍摄日期填入「日期」，之后仍可手动修改（编辑已有回忆不触发）
- 应用内更新：下载过程中离开「关于」页不再中断下载，重进页面可继续看到下载进度
- 通知偏好：切换某个开关时，其它开关与总开关不再闪一下变灰

## 0.1.0+2

- 家务活插件
- 成员管理与扫码加入家庭
- 外观主题切换（浅色 / 深色 / 跟随系统）
- 支持上传 / 修改用户头像
- 应用内检查更新：在「我的 → 关于「窝」」可检查并下载安装最新版本
- 首页移除卡片下方的虚线「添加插件」入口，统一用右下角加号添加
- 首页编辑态拖动卡片松手后，受影响的卡片平滑动画移动到新位置
- 通知偏好：在「我的 → 设置 → 通知偏好」可总开关系统推送，并按来源（家庭动态 / 各插件）单独控制是否推送
